# GitHub personal access token (optional but recommended to avoid rate limiting)
# Create one at https://github.com/settings/tokens — no special scopes needed
GITHUB_TOKEN=ghp_your_token_here

# MongoDB connection (only change if running MongoDB externally)
MONGODB_URL=mongodb://localhost:27017
